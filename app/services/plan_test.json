{
    "steps": [
      {
        "task": "Find out what product does the user want to build",
        "inputs": [
          {
            "name": "$user_query",
            "value": "['website', 'webApp', 'mobileApp', 'desktopApp']",
            "description": "determine which item the user wants to build"
          }
        ],
        "tool_id": "product_tool",
        "output": "$product_type"
      },
      {
        "task": "Find out what information does the ",
        "inputs": [
          {
            "name": "$product_type",
            "description": "What is required to build the $product_type?"
          }
        ],
        "tool_id": "requirement_tool",
        "output": "$requirements",
      }
    ]
  }